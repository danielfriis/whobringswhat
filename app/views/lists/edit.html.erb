<h1>Edit list</h1>

<%= form_for @list do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= render 'fields', f:f %>
	<p>
		<%= f.submit 'Save', class: "btn btn-primary" %>
	</p>
<% end %>